# Dependencies for the Food GI-RAG Web UI.
#
# The GI query path always embeds in-process (Qwen3-Embedding-0.6B, via
# sentence-transformers/transformers below) — embedding.embed_endpoint in
# the config is used only by the offline upload/build tooling, not by api.py.

openai>=1.30
azure-cosmos>=4.7
azure-identity>=1.17
pyyaml>=6.0
numpy>=1.26
httpx>=0.27
aiohttp>=3.9          # required by azure-cosmos's async transport

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# In-process embedding (Qwen3-Embedding-0.6B). GPU used automatically if
# available; requires python3.12-dev on the host for Triton to JIT-compile
# its CUDA helper, or embedding silently falls back to CPU (~200ms vs ~30ms).
sentence-transformers>=3.0
transformers>=4.40

# index.mode: local only (GPU-resident Graph Index snapshot + local BM25
# full-text search). Not needed for index.mode: cosmos.
pyarrow>=14.0
pandas>=2.0
rank_bm25>=0.2
