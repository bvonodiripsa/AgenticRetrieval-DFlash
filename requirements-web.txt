# Dependencies for the Food KG-RAG Web UI.
# Embeddings use a configured HTTP endpoint (e.g. Ollama) — no torch required.

openai>=1.30
azure-cosmos>=4.7
azure-identity>=1.17
pyyaml>=6.0
numpy>=1.26
httpx>=0.27

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Optional in-process embedding fallback (requires torch; NOT needed when
# embedding.embed_endpoint is set). Uncomment to enable:
#   sentence-transformers>=3.0
#   transformers>=4.40
